# Badr World Native Android V4 Fixed

This version is defensive:

- analysis_options.yaml is overwritten without flutter_lints include.
- any remaining `minHeight: 12` in `LinearProgressIndicator` is removed.
- GitHub Actions also repeats the cleanup before analyze.
- Upload this ZIP using ChatGPT Project Sync V10.

Important: choose this exact file: badr_world_native_android_v4_fixed.zip
