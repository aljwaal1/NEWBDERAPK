name: badr_world_native
description: Native Android Flutter app for Badr World.
publish_to: 'none'
version: 3.0.0+3

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.0.2
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_lints: ^4.0.0
